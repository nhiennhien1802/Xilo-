// types/database.types.ts
//
// Đây là kiểu dữ liệu TẠM THỜI để code build được ngay.
// Để có gợi ý code chính xác 100% theo đúng schema thật trong Supabase,
// sau khi deploy xong bạn nên chạy lệnh sau (cần cài Supabase CLI):
//
//   npx supabase gen types typescript --project-id <project-ref> > types/database.types.ts
//
// rồi thay thế nội dung file này.

export type Database = any;
